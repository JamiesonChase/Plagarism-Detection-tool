public class PirateTranslator {
    V[] V = {S, S, S, S, S,
			S, S, S, S,
			S, S,
			S, S, S,
			S, S, S, S};
    V[] V = {S, S, S, S, S,
			   S, S, S, S,
			   S, S,
			   S, S, S,
			   S, S,
			   S, S};
    V[] V = {S, S, S};
    V[] V = {S, S, S};
    V[] V = new V[25];
    int V = 0;
    public V F(V V) {
		for (int V = 0; V < (V.length); V++) {
			V = (V.replace(V[V], V[V]));
			}
		return V;
	 }
}